# ⚠️  Giới hạn trên là BẮT BUỘC: ragas 0.4.x import langchain_community.chat_models.vertexai,
#     module này đã bị xóa khỏi langchain-community 0.4.x → Bước 3 sẽ crash khi import.
# Đã kiểm chứng chạy đủ 4 bước với Python 3.12 + langchain 0.3.30 + ragas 0.4.3.

# ── LangChain core ────────────────────────────────────────────────
langchain>=0.3.0,<0.4
langchain-core>=0.3.0,<0.4
langchain-community>=0.3.0,<0.4
langchain-text-splitters>=0.3.0,<0.4

# ── LLM Providers (cài tất cả để hỗ trợ đa provider) ─────────────
langchain-openai>=0.3.0,<0.4          # OpenAI + OpenRouter
langchain-anthropic>=0.1.0,<1.0       # Anthropic Claude
langchain-google-genai>=1.0.0,<3.0    # Google Gemini
langchain-ollama>=0.1.0,<1.0          # Ollama (local)

# ── LangSmith ─────────────────────────────────────────────────────
langsmith>=0.2.0

# ── Vector store ──────────────────────────────────────────────────
faiss-cpu>=1.7.0

# ── RAG Evaluation ────────────────────────────────────────────────
ragas>=0.4.0
datasets>=2.0.0                  # required by RAGAS internally
numpy>=1.25.0

# ── Guardrails AI ─────────────────────────────────────────────────
guardrails-ai>=0.5.0

# ── Utilities ─────────────────────────────────────────────────────
python-dotenv>=1.0.0
openai>=1.0.0                    # OpenAI SDK (used by langchain-openai)
tiktoken>=0.5.0                  # token counting
